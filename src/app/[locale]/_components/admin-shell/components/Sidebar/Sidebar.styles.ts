const styles = {
  root: 'flex flex-col border-r border-border bg-paper overflow-y-auto',
  header: 'flex items-baseline gap-2 px-6 py-6 border-b border-border',
  modules: 'flex flex-col gap-2 px-3 py-4',
  // mt-auto pins the toggle to the bottom of the flex column.
  footer: 'mt-auto border-t border-border px-3 py-4',
};

export default styles;
