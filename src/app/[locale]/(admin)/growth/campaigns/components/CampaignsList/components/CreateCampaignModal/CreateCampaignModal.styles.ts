const styles = {
  modalContent: 'flex flex-col gap-4 w-full max-w-lg',
  grid: 'grid grid-cols-2 gap-3',
  segment: 'flex flex-col gap-3 rounded-lg border border-border bg-background p-3',
  actions: 'flex items-center justify-end gap-2',
};

export default styles;
