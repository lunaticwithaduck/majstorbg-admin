const styles = {
  modalContent: 'flex flex-col gap-4 w-full max-w-2xl',
  preview: 'flex flex-col gap-2 rounded-lg border border-border bg-background p-3',
  previewBody: 'whitespace-pre-wrap',
  vars: 'flex items-center gap-2 flex-wrap',
  actions: 'flex items-center justify-end gap-2',
};

export default styles;
